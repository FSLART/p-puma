name: CI

on:
  push:
    branches: [ "dockerization" ]
  pull_request:
    branches: [ "dockerization" ]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ros:humble-ros-base
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: src/p-puma

      - name: Install build tools
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            git \
            python3-rosdep \
            python3-colcon-common-extensions

      - name: Initialize rosdep
        run: |
          rosdep init || true
          rosdep update

      - name: Clone external ROS packages
        run: |
          git clone -b dev https://github.com/FSLART/lart_msgs.git src/lart_msgs
          git clone https://github.com/FSLART/lart_common.git src/lart_common

      - name: Install ROS dependencies
        shell: bash
        run: |
          source /opt/ros/humble/setup.bash
          rosdep install --from-paths src --ignore-src -r -y

      - name: Build workspace
        shell: bash
        run: |
          source /opt/ros/humble/setup.bash
          colcon build --symlink-install

      - name: Run tests
        shell: bash
        run: |
          source /opt/ros/humble/setup.bash
          colcon test --packages-select p-puma
          colcon test-result --all
